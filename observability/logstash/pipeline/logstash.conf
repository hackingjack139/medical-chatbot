input {
  gelf {
    port => 12201
  }
}

filter {
  mutate {
    add_field => {
      "app" => "%{container_name}"
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "medical-chatbot-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
